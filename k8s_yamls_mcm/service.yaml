apiVersion: app.ibm.com/v1alpha1
kind: Deployable
metadata:
  annotations:
    app.ibm.com/is-local-deployable: "true"
  name: lab1-tradr-service
spec:
  template:
    apiVersion: v1
    kind: Service
    metadata:
      name: lab1-tradr-service
    spec:
      selector:
        app: lab1-tradr
      ports:
        - protocol: TCP
          port: 80
          targetPort: 3000
